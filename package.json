{
    "author": "nowtwo-llc",
    "name": "@nowtwo-llc/quick-switcher",
    "description": "nowtwo-llc quick switcher repo for the main application. Heavily adapted from Matt Light's quick switcher project.",
    "repository": "git@github.com:nowtwo-llc/quick-switcher.git",
    "license": "UNLICENSED",
    "publishConfig": {
        "registry": "https://npm.pkg.github.com/"
    },
    "scripts": {
        "build": "npm run build:js && npm run build:css",
        "build:js": "r.js -o build.js",
        "build:css": "node-sass --output-style compressed --include-path src src/quick-switcher.scss dist/quick-switcher.min.css",
        "update": "npm-check-updates -u",
        "watch": "concurrently 'npm run watch:js' 'npm run watch:css'",
        "watch:js": "npm-watch build:js",
        "watch:css": "npm-watch build:css",
        "test": "faucet test/"
    },
    "version": "1.1.0",
    "watch": {
        "build:js": {
            "patterns": [
                "src"
            ],
            "extensions": "js",
            "quiet": false
        },
        "build:css": {
            "patterns": [
                "src"
            ],
            "extensions": "scss",
            "quiet": false
        }
    },
    "files": [
        "dist/"
    ],
    "devDependencies": {
        "almond": "^0.3.3",
        "concurrently": "^8.1.0",
        "faucet": "^0.0.4",
        "node-sass": "^9.0.0",
        "npm-check-updates": "^16.10.12",
        "npm-watch": "^0.11.0",
        "requirejs": "^2.3.6",
        "tape": "^5.6.3",
        "text": "github:requirejs/text"
    }
}
